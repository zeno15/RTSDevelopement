#include "Game.h"

int main()
{
	sGame.initialise(sf::Vector2u(1440, 900), "RTS Developement - Map Editor");
	//sGame.initialise(sf::Vector2u(1366, 768), "RTS Developement - Map Editor");
	
	return 0;
}